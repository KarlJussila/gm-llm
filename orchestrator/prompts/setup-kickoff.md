A new campaign is being created from an empty directory. Load the `campaign-setup` skill and follow its task list.

Work with the player conversationally: when a step calls for their input, ask and wait for their reply before moving on.
