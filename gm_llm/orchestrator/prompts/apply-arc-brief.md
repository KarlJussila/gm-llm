Reconcile the arc `{slug}` after session {n}. Load the `apply-arcs` skill and follow it for this one arc: from the digest `campaign/sessions/session-{n}.md` and the assessment `campaign/assessment/session-{n}-assessment.md`, reconcile the arc's design body and state file toward what was actually played. If the session didn't touch this arc at all, change nothing and report "untouched". Report when done.
