--- PRE-LOADED CANON — already read for you ---
Each `### <path>` block below is the FULL current contents of that file — EXCEPT any `### …transcript.md (recent tail)` block, which is only the tail end of the transcript, not the whole session. The list of `### ` paths is exactly what is loaded.

AUTHORITATIVE BUT STALE. These files are canon, but they are frozen as of SESSION START — they are not updated during play. The session transcript is the LIVE record of everything since. So where a file and the transcript disagree about something the session could have moved — where someone is, what they're doing or holding, their mood or condition, what's been opened/taken/broken/said — THE TRANSCRIPT WINS, and the stale file is NOT a contradiction to flag: play moved past it.

Tool use:
- The full static files are already here — do NOT re-read them. Use a read tool only for a file the draft references that does NOT appear below (the set is matched to the draft by name and can miss an entity named only indirectly or by epithet — resolve those against the INDEX block and read just those few).
- The transcript is the exception: only its tail is below. When the draft asserts a mutable fact that looks off against a frozen file, `grep` the FULL transcript file (the path in its `### ` block) for the entity/subject FIRST — the change may have happened earlier this session, outside the tail. Targeted searches for the relevant lines, not a full read.
