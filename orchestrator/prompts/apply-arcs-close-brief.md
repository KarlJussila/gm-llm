Close out session {n}'s arc pass — every arc has just been reconciled individually. Three jobs, from the digest `campaign/sessions/session-{n}.md` and the assessment:

1. **New minor arcs.** If play birthed a subplot that deserves real structure (committed answers + turning points), load `arc-design` and author it as a proper minor arc (`tier: minor`, `hooks-into:` the major), registered in `INDEX.md`. A lighter lead that isn't yet a story stays a thread.
2. **Threads dashboard.** Reconcile `campaign/state/threads.md` (format per `canon-conventions`): open the threads play created, advance the ones it moved, resolve the ones it closed, and promote any that crossed into a real story via job 1.
3. **Consistency sweep.** Confirm every cross-arc/thread hook references both ways, the `INDEX.md` Arc rows are current (status, one-line), no arc was left stale or self-contradictory, and at least one arc advanced this session.

Report when done.
